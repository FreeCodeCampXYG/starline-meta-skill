# v3.0.3 作者规范

- 默认中文作者名：墨点星痕。
- 默认英文名：starline。
- frontmatter 默认值：`author: "墨点星痕 (starline)"`。
- README、manifest、interface、creation handoff 与 copyright 必须保持一致。
- 用户明确指定其他作者时，用户值覆盖默认值。

原规则默认 owner 为 `Starline`，并非“乔木”。本次将品牌名细化为可读的中英文作者身份，同时保留 `starline-` 技能命名前缀。
